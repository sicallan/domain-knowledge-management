# Archon repo configuration — committed to git.
# See .claude/skills/archon/references/repo-init.md for the full reference.

assistant: claude

worktree:
  baseBranch: main
  copyFiles:
    - .env
    - .env.local

defaults:
  loadDefaultCommands: true
  loadDefaultWorkflows: true
