Implement ONLY Feature 2.3 — Decision-specific extraction — per docs/features/phase-2/03-decision-extraction.md. TDD-first per its §8. End by opening a PR yourself (title: "feat(phase-2): decision-specific extraction + cardinality gate + golden eval (2.3)", body closing #38).

BUILD ON the existing code; do NOT re-scaffold. The Phase 1 + 2.2 Python extraction pipeline is in modules/enrichment (just-merged 2.2 behaviour pass is your template — mirror its structure exactly). Reuse: the gateway/chunking/confidence/emission/entity_resolution/evaluation modules, the PromptLibrary, the SchemaValidator, and the FakeGateway scripted-router test pattern (see modules/enrichment/tests/conftest.py + test_behaviour_*.py). Grep modules/ before adding anything.

CRITICAL — diff the feature doc against what already exists (the doc overstates what's missing):
- Decision ENTITIES ALREADY EXTRACT. "Decision" is already in PHASE_0A_L1_TYPES (models.py) and has a prompts/templates/decision.v1.md template shipped in Phase 1. Task-breakdown item 1 ("add Decision to targetTypes") is DONE. Do not re-add it.
- The Decision schema (schemas/inventory/L1/decision.schema.json) is shipped: required name, decisionType (enum automated|manual|hybrid — NOT `type`; `type` is the base-entry discriminator const), outcomes (≥1); optional inputs, owner, frequency, latencyBudget. The feature doc §2 says `type` loosely — use decisionType (same convention as Event's eventType). Rules/ReferenceData/invariants are RELATIONSHIPS, not entry fields.
- The six decision edges already have schemas/relationships/decision-specific.schema.json (shipped 2.1): evaluates(Decision→Rule|BusinessInvariant), consumes(Decision→ReferenceData), constrainedBy(Decision→BusinessInvariant), triggeredBy(Event|OrchestrationStep→Decision), produces(Decision→Event|Command|StateTransition), realizedBy(Decision→Service|Component).

So 2.3's actual work is:
1. Decision-specific RELATIONSHIP extraction + an endpoint-type gate. Add `validate_decision_relationship(kind, source_type, target_type)` to schema_validation.py, mirroring the existing `validate_behavioural_relationship` (validate against decision-specific.schema.json's $id). Extend the pipeline _emit relationship gate (the one added in 2.2) so decision-specific edges are validated the same way: wrong-typed endpoints, or endpoints referencing a not-yet-committed cross-pass placeholder (e.g. evaluates→a Rule not extracted, or triggeredBy from a Step not extracted), are QUARANTINED to the review queue and counted (stats.quarantined) — never written dangling (D-P2.5). This RESOLVES the invokes(Step→Decision) placeholders 2.2 quarantines: when the Decision pass runs over the same docs, those Decisions now commit. Do NOT edit the 2.2 behavioural branch or the Phase 1 structural path.
2. The CARDINALITY/CONDITIONAL gate (acceptance criteria 3-5): per committed Decision, enforce evaluates≥1, produces≥1, and decisionType=automated ⇒ triggeredBy≥1. These rules are DEFINED ONCE in modules/schema/src/relationships.ts (checkMinimum / checkAutomatedDecisionTrigger; DEFAULT_DEFS evaluates min 1, produces min 1) — D-P2.2 "defined once, enforced twice". The Python emit-gate is the second enforcement point: treat the TS registry as the canonical source of the numbers (reference it in a comment; do not invent divergent numbers). A Decision violating a cardinality/conditional rule is FLAGGED → routed to the review queue + counted in stats (a new stat e.g. cardinalityFlagged, or reuse quarantined with a clear log) — NOT auto-merged, NOT hard-dropped (D-P2.5 + D-P1.5 two-tier).
3. Tiered escalation (criterion 6, D-P1.1 / spec 005 Decision 4): on a re-run with escalate=True the harder decision extraction routes to claude-opus-4-8 and metadata.model reflects it. Test deterministically with FakeGateway (assert models_used / the run metadata) — no network. The escalation knobs already exist on ExtractionConfig (escalate, escalateModel, autoMergeBand).
4. evals/payments-decision-golden/ labelled golden dataset + extend evaluate() reporting to Decision + the six decision-specific relationship types. Mirror evals/payments-behaviour-golden/ exactly (dataset.json with documents[], expectedEntities, expectedRelationships, and an embedded extractionScript the deterministic test replays through FakeGateway; markdown docs under documents/). Note: load_golden_dataset builds each doc as ONE chunk whose sectionTitle = the descriptor "title" — so the extractionScript section keys must equal the descriptor titles (not the markdown headings).
5. Optionally add new versioned decision prompt templates per source doc-type (decision-log / rulebook / policy / technical-design) ONLY if the single decision.v1.md is insufficient — additive new files (prompts/templates/<name>.v1.md), never edit the engine (D-P2.7, spec 005 Decision 1).

ACCURACY FLOORS ARE LOCKED in docs/phase-2/decisions.md D-P2.1 (the feature doc §11 "TBD" is RESOLVED — do not re-open it):
- Decision entities: precision ≥ 0.85, recall ≥ 0.65, F1 ≥ 0.74, auto-merge-band precision (confidence ≥ 0.8) ≥ 0.92 (the strictest bar in the system).
- Decision-specific relationships: precision ≥ 0.75, recall ≥ 0.55, F1 ≥ 0.63, auto-merge-band precision ≥ 0.88, per-type F1 ≥ 0.55 for types with ≥ 5 golden instances.
Build the golden set so the DETERMINISTIC (FakeGateway, scripted) eval clears these — that is the CI gate. The real-Claude eval is @pytest.mark.llm and auto-skips without ANTHROPIC_API_KEY (track real-world verification as a follow-up issue; never a CI gate — no secrets/services in CI).

Command endpoint wrinkle: produces→Command targets a DomainConcept with conceptType="command" (D-P2.3), not a top-level Command type. To sidestep this in the golden set prefer produces→Event/StateTransition (as 2.2 did); only exercise →Command if you map the endpoint type correctly.

Decision-vs-Rule boundary (feature doc §11): a Decision USES rules but is not a rule. Don't let extraction collapse the two; encode the distinction in prompts + golden labels; add at least one adversarial fixture.

OCP — CLOSED surfaces (do not touch): ExtractionPipeline / LLMGateway signatures; the JSONL fixed core + entities/relationships file split (spec 003); the Decision schema + cardinality rules (owned by Feature 01 / the registry); the 2.2 behaviour pass; the Phase 1 structural pass. OPEN: new prompt templates, new relationship endpoint validators, eval reporting — all additive.

OUT OF SCOPE: behaviour-structure extraction (2.2, merged); the Decision Inventory / Behaviour Flow views (2.4); loading + cross-layer traversal (2.5); reasoning over decisions (Phase 4/5); any change to the gateway, JSONL fixed core, or file split.

TDD — write these six test files FIRST (per §8), confirm RED, then implement to GREEN: test_decision_prompts.py, test_decision_edges.py, test_decision_cardinality_gate.py, test_decision_escalation.py, test_decision_eval.py, test_decision_end_to_end.py.

CONVENTIONS: British spelling in domain language/identifiers/text (Authorisation, Realisation, Behaviour, prioritise). Every asserted fact evidenced + versioned. Verify green before the PR: in modules/enrichment run `.venv/bin/ruff check .`, `.venv/bin/mypy`, `.venv/bin/pytest -m 'not llm'`; and `pnpm run validate` at the repo root. Then commit and open the PR.
