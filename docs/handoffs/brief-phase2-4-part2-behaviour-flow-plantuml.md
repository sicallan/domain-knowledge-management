Implement ONLY **Part 2 of Feature 2.4 — the Behaviour Flow PlantUML RENDER + the cross-adapter end-to-end test** — per docs/features/phase-2/04-behaviour-flow-view.md. **Part 1 (the `BehaviourFlowProjector` + the `BehaviourFlowView` data type, merged in PR #47) is DONE and on `main` — do NOT reimplement it.** This Part 2 covers acceptance criteria **5 (PlantUML render)** and **7 (cross-adapter parity)** only. TDD-first. End by opening a PR yourself — title: "feat(phase-2): Behaviour Flow PlantUML render + cross-adapter e2e (2.4 part 2)"; body references "Part 2 of #39" and **"Closes #39"** (Part 2 is the last part, so it closes the issue).

SCOPE — Part 2 delivers a tested, view-consuming PlantUML renderer plus the parity test:

1. A **pure renderer** `renderBehaviourFlowPlantUml(view: BehaviourFlowView): string` in a NEW file `modules/view-projection/src/behaviour-flow-plantuml.ts`. It consumes the **Part 1 `BehaviourFlowView`** (import the type from `./types`) — it takes the projected view as input, it does NOT re-query the graph. Export it from `modules/view-projection/src/index.ts` (additive).
   - It must emit **valid PlantUML** in which: every step appears in `sequence` order; each step's `emits`/`consumes` events and its outgoing `transitions` (fromState → toState, with guardCondition when present) are shown; and **decision-point steps (`isDecisionPoint: true`) are visually DISTINCT** — render them as a highlighted node (gold hexagon or an activity `if`/diamond) carrying the decision's name, `type` (automated|manual|hybrid) and its outcome branches. This is the feature's stated value (criterion 5: a compliance officer can spot where regulation bites).
   - The flow header (`view.flow`: id/name/trigger/owningService?) should appear as the diagram title/legend.

2. **WHY a module, not `demo/`:** the repo's vitest config only runs `modules/*/test/**/*.test.ts`, and criterion 5 requires a golden test — so the renderer + its tests MUST live under `modules/view-projection`, not the demo. **LEAVE `demo/src/behaviour-flow-exporter.ts` EXACTLY AS-IS** — it is a deliberately demo-local whole-subgraph picture (reads `traverse` directly) and is out of scope. Mirror its **visual language** (gold-hexagon Decisions `#FFD54F`, bold-amber decision edges `#F57F17`, blue step boxes, yellow event ovals, purple state transitions, `left to right direction`) so the two pictures look consistent — but the new renderer consumes the typed `BehaviourFlowView`, not a subgraph.

TDD — write these TWO test files FIRST (Part 2 only), confirm RED, implement to GREEN:

- `modules/view-projection/test/behaviour-flow-plantuml.test.ts` — build a known `BehaviourFlowView` fixture (a flow with ordered steps, an emitted + a consumed event, a state transition, and one decision-point step with a decision + outcomes). Assert the rendered string: contains `@startuml` … `@enduml`; steps appear in `sequence` order; every event (emits + consumes) and every transition is rendered; the decision-point step is rendered distinctly and carries the decision name, `type` and outcome labels. Prefer a **golden-string** compare plus a few targeted-substring assertions for robustness. Reuse the Part 1 fixtures/helpers in `modules/view-projection/test/helpers.ts` where they fit (the same seeded flow shape).
- `modules/view-projection/test/behaviour-flow-end-to-end.int.test.ts` — the cross-adapter parity test (criterion 7): seed one flow via the graph loader/adapter → run `BehaviourFlowProjector.project` → `renderBehaviourFlowPlantUml`, and assert the SAME PlantUML output against the **in-memory adapter AND the Neo4j adapter**. The Neo4j path MUST **auto-skip when `NEO4J_URI` is unset** — follow the existing skip precedent (the 1 skipped test in `modules/knowledge-graph/test/graph-port.contract.test.ts`, and the `.int.test.ts` pattern in `modules/query-interface/test/query-over-loaded-graph.int.test.ts`). CI must stay green with NO services/secrets; the real Neo4j parity run is tracked as a follow-up only.

OUT OF SCOPE (do NOT do): re-implementing the `BehaviourFlowProjector` or the `BehaviourFlowView` type (Part 1 — already merged on `main`); modifying the `ViewEngine`/`ViewProjector` interfaces, the Query Interface contract, the Domain Map projector, or `demo/src/behaviour-flow-exporter.ts`; UI rendering; the Decision Inventory view; GraphQL serving; a Mermaid exporter; new query primitives.

CONVENTIONS: British spelling in domain language/identifiers/text (Behaviour, Authorisation, prioritise). Renderer + types additive only (spec 007 Open Q2). CI green without secrets/services (Neo4j path auto-skips without `NEO4J_URI`). Verify green before the PR: `pnpm run validate` (typecheck + lint + test) at the repo root. Then commit and open the PR (Closes #39).
