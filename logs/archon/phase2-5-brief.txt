Implement ONLY Phase 2.5 — Cross-layer linking (GitHub issue #40), exactly as specified in docs/features/phase-2/05-cross-layer-linking.md. This is the integrating step of Phase 2: make the decision-specific + behavioural edges persisted with validated endpoints and traversable in both directions across L1/L2/L3. Read that feature doc and docs/phase-2/decisions.md before writing any code. British spelling throughout (Behaviour, Realisation, prioritise).

TDD-FIRST — write these failing tests before implementation, exactly as §8 of the feature doc lists them (one criterion each):
- modules/loaders/test/cross-layer-edge-load.test.ts — valid edge → createEdge; wrong-typed endpoint → rejected + reported (not silently dropped); schema-invalid edge excluded (criterion 1).
- modules/loaders/test/link-time-cardinality.test.ts — evaluates ≥ 1, produces ≥ 1, automated ⇒ triggeredBy, belongsTo N:1 enforced at load (criterion 2).
- modules/loaders/test/dangling-edge-policy.test.ts — edge with a missing endpoint routed to review/quarantine, NOT committed; counted in stats (criterion 6).
- modules/query-interface/test (contract) traversal-cross-layer — cross-layer findPaths/traverse return correct paths; bidirectional navigation out/in/both (criteria 3–5).
- modules/query-interface/test (contract) adapter-parity-traversal — identical results on in-memory and Neo4j adapters; AUTO-SKIP Neo4j when NEO4J_URI is unset (criterion 7).
- integration cross-layer-end-to-end — JSONL edges from the Features 02/03 fixtures → loader → graph → cross-layer traversal returns the expected connected subgraph; EdgeCreated events logged (criterion 8).

BUILD ON the existing foundation — do NOT re-scaffold modules, ports, or interfaces:
- modules/schema/src/relationships.ts already exports RelationshipTypeRegistry with checkMinimum() and checkAutomatedDecisionTrigger(), plus the decision-specific + structural edge defs (evaluates, consumes, constrainedBy, produces, triggeredBy, realizedBy, implements, belongsTo, emits, evidencedBy) and BEHAVIOURAL_RELATIONSHIP_DEFS (with sourceTypes/targetTypes endpoint constraints) + registerBehaviouralRelationships(). This registry is the SINGLE SHARED RULE SET — reuse it; do NOT fork or re-implement cardinality logic (D-P2.2: defined once, enforced twice).
- The regulatory cross-layer edge types (satisfiedBy, obliges, exposes) and structural usesReferenceData/governs are NOT yet registered. ADD them ADDITIVELY to the registry (a new *_RELATIONSHIP_DEFS list + register helper, mirroring the behavioural pattern). Register satisfiedBy → ProjectSpec (L2) as a *type* but do NOT gate on L2 instances existing (L2 schemas arrive in Phase 3 — forward-compatible per the decisions doc).
- modules/loaders/src/graph-loader.ts (GraphLoader, writes through @dkm/knowledge-graph GraphPort) currently treats a missing endpoint as a non-retriable LoadResult.errors[] entry. CHANGE this: before createEdge, validate endpoint TYPES against the registry and enforce cardinality/conditional rules at link time via the shared registry; route dangling edges (endpoint not yet present) AND cardinality/type-invalid edges to a QUARANTINE / review-queue sink and COUNT them (D-P2.5: never drop, never commit a dangling edge). Introduce only the MINIMAL quarantine routing the loader needs (e.g. a counted quarantine list surfaced in LoadResult) — do NOT build a separate review-queue service or module.
- modules/knowledge-graph: GraphPort + in-memory and Neo4j adapters already emit EdgeCreated GraphMutationEvent on createEdge. Just CONFIRM the event fires on cross-layer createEdge; do not modify the port.
- modules/query-interface already implements traverse() and findPaths() with direction: 'out'|'in'|'both', edgeTypes filter, and maxDepth (clamped by maxDepthCap). VERIFY they handle the new edge types with bidirectional nav + node/edge-type filters and add cross-layer fixtures + tests — do NOT rebuild the query service. Enforce maxDepth + a result cap with a 'truncated' marker for deep cross-layer paths (path-explosion guard).
- Add cross-layer fixtures spanning L1↔L3 plus a regulatory→decision trace (RegulatoryRequirement —satisfiedBy→ Decision —evaluates→ Rule). Reuse the Features 02/03 JSONL fixtures where they exist.

OCP (honour §10): OPEN = new cross-layer edge types supported by adding their schemas/registry defs, handled generically by type; new graph adapters behind the port. CLOSED = GraphPort and QueryService interfaces, the JSONL relationship contract, and the bidirectional-traversal guarantee. Adding an edge type must NOT modify loader/query core or the port — it must be driven by the relationship/registry.

OUT OF SCOPE — do not touch: defining relationship schemas (Feature 01) or extracting edges (Features 02/03); the Compliance Matrix / Gap Analysis views (Phase 3, spec 007); L2 population / VendorProduct/ProjectSpec schemas (Phase 3); impact-assessment traversal/scoring (Phase 4, spec 009). No workflow engine — loading stays synchronous/in-process (D-P2.6).

CI MUST stay green with NO secrets or services: the in-memory adapter and deterministic tests are the gate; the Neo4j parity test auto-skips unless NEO4J_URI is set. Run `pnpm run validate` (typecheck + lint + test) and ensure it passes before finishing.

When implementation is complete and `pnpm run validate` is green, commit and OPEN A PULL REQUEST (base main) summarising the cross-layer linking work and referencing issue #40.
