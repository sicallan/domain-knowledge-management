{
  "name": "@dkm/loaders",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Loader Port (spec 003 §Loader Port Interface): abstract LoaderPort, contract test suite, an in-memory stub, the graph-loader (writes through the spec-002 graph port), and the orchestrator.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@dkm/schema": "workspace:*",
    "@dkm/knowledge-graph": "workspace:*"
  }
}
